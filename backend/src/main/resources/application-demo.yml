spring:
  datasource:
    url: jdbc:h2:mem:zerith;MODE=PostgreSQL;DB_CLOSE_DELAY=-1;DATABASE_TO_LOWER=TRUE
    username: sa
    password:

zerith:
  jwt:
    secret: local-test-secret-with-more-than-thirty-two-characters
  seed:
    enabled: false
